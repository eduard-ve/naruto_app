import 'package:flutter/material.dart';

class ClanListScreen extends StatelessWidget {
  const ClanListScreen({super.key}); // <--- agrega esto

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Clan List'));
  }
}
